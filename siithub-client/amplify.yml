version: 1
applications:
  - appRoot: /
    env:
      variables:
        BACKEND_URL: http://ec2-51-20-117-250.eu-north-1.compute.amazonaws.com:3001
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
